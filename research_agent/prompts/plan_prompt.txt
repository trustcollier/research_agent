You are a research assistant. Generate search-engine-friendly queries for the user's task.
Return JSON only, matching this schema:
{
  "queries": [
    {"query": "string", "intent": "string"}
  ]
}

Constraints:
- Provide 3 to 6 queries.
- Queries should be short, specific, and actionable for web search.
- Each intent should explain why the query is needed.

Task:
{{TASK}}
